import { Section, Switcher, Preview, Snippet } from '@wpmudev/storybook';
import { Recipient } from '../../src';
import dedent from 'dedent';

import Status from './Examples/Status.mdx';
import Types from './Examples/Types.mdx';

export const sampleCode = dedent`
import React from 'react';\n
import { Recipient } from '@wpmudev/sui-react';\n
const Example() => {
	return <Recipient userName="Recipient name" userEmail="email@example.com" />;
}\n
export default Example;
`;

<Section contained={true} style={{ paddingTop: 0 }}>
	<p>Below is the default form of a recipient, used for most cases.</p>
	<Preview wrapper={true}>
		<Recipient userName="Recipient name" userEmail="email@example.com" />
	</Preview>
	<Snippet language="js">{sampleCode}</Snippet>
</Section>

<Section style={{ paddingTop: 0 }}>

<Switcher>
	<div label="Types" value="types">
		<Types />
	</div>
	<div label="Status" value="status">
		<Status />
	</div>
</Switcher>

</Section>
