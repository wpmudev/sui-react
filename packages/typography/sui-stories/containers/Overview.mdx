import { Source } from '@storybook/addon-docs';
import { Docs } from '@wpmudev/docs-elements';
import dedent from 'dedent';

export const codeInstall = dedent`
npm install @wpmudev/css-typography --save-dev
`;

export const codeUsage = dedent`
@forward "~@wpmudev/css-typography/typography";
`;

<span></span>

<Docs.Section title="Installation">
	<p>
		There are two ways to access Typography stylesheets: install the
		complete CSS Framework package or the single package that contains only
		the Typography stylesheets you need.
	</p>
	<p>
		Typography single package contains three files in different formats:
		CSS, Minified CSS, and Sassy CSS (Scss).
	</p>
	<Source dark language="bash" format={false} code={codeInstall} />
</Docs.Section>

<Docs.Section title="Usage">
	<p>
		Import typography stylesheet inside an existing SCSS file in your
		project; do the following:
	</p>
	<Source dark language="css" format={false} code={codeUsage} />
</Docs.Section>
