import { Section, Code, PackageInstallGuide, PropSection } from "@wpmudev/sui-docs"

<PackageInstallGuide packageName="@wpmudev/sui-uploader" />

<Section title="Props" border={true}>

    	<PropSection title="title" type="string" defaultValue="{{default}}">
    		<p>Upload Button Title</p>
    	</PropSection>

    	<PropSection title="multiple" type="boolean" defaultValue="{{default}}">
    		<p>When set to <Code isSmall={true}> true </Code>, the component will allow for multiple files selection </p>
    	</PropSection>

    	<PropSection title="accept" type="boolean" defaultValue="{{default}}">
    		<p>The allowed <a href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/file#accept" target="_blank"> file types</a> to upload.</p>
    	</PropSection>

    	<PropSection title="allowDragAndDrop" type="boolean" defaultValue="{{default}}">
    		<p>When set to <Code isSmall={true}> true </Code>, Users will be able to use drag and drop to upload files </p>
    	</PropSection>

    	<PropSection title="onChange" type="function" defaultValue="{{default}}">
    		<p>A callback function to be invoked when a file is uploaded, the first parameter is an <Code isSmall={true}> array </Code> of the uploaded files objects</p>
    	</PropSection>

</Section>
