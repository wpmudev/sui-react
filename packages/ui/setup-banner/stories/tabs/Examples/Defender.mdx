import { Section, Preview, Snippet } from "@wpmudev/sui-docs"

import { SetupBanner } from "../../../src"

import dedent from "dedent"

export const DefenderCode = dedent`
        <SetupBanner
            type="defender"
            title="Defender"
            description="Monitor the important elements of site security from one dashboard. Run security scans, manage your blocklists / firewalls, enable 2FA, and action hardening recommendations with one click."
        />
`

<Section contained={true} border={true} style={{ paddingRight: 0, paddingLeft: 0 }}>
  <Preview wrapper={true}>
    <SetupBanner type="defender" title="Defender" description="Monitor the important elements of site security from one dashboard. Run security scans, manage your blocklists / firewalls, enable 2FA, and action hardening recommendations with one click." />
  </Preview>

  <Snippet language="js">
    {DefenderCode}
  </Snippet>
</Section>
