import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection } from "@wpmudev/sui-docs"

<PackageInstallGuide packageName="@wpmudev/sui-selector" />

<Section title="Props" border={true}>
	<PropSection title="title*" type="string" defaultValue="{{default}}">
    	<p>The main heading text for the Selector </p>
    </PropSection>

	<PropSection title="description*" type="string" defaultValue="{{default}}">
    	<p>An additional text that accompagnes the title </p>
    </PropSection>

    <PropSection title="iconOrBrandUrl*" type="string" defaultValue="{{default}}">
    	<p> Either an image url or an icon to be displayed, this wll appear beside the title </p>
    </PropSection>

    <PropSection title="label*" type="string" defaultValue="{{default}}">
    	<p>A label text for the hidden radio input ( for screen readers ) </p>
    </PropSection>

    <PropSection title="name*" type="string" defaultValue="{{default}}">
    	<p>A label text for screen readers</p>
    </PropSection>

    <PropSection title="value*" type="string | number" defaultValue="{{default}}">
    	<p>Value of the radio input</p>
    </PropSection>

    <PropSection title="isChecked*" type="boolean" defaultValue="{{default}}">
    	<p>Whether the radio input is checked or not</p>
    </PropSection>

    <PropSection title="isDisabled*" type="boolean" defaultValue="{{default}}">
    	<p>Whether the radio input is disabled or not</p>
    </PropSection>

    <PropSection title="isPro*" type="boolean" defaultValue="{{default}}">
    	<p>When set to <Code small={true}>true</Code>, the Selector appears in the pro variation</p>
    </PropSection>

    <PropSection title="alignment*" type="string" defaultValue="{{default}}">
    	<p>Specifies the alignment of the content, it could be one of two values:</p>
    	<List>
    		<span label={<Code small={true}>left</Code>}/>
    		<span label={<Code small={true}>center</Code>}/>
    	</List>
    </PropSection>

    <PropSection title="allowRemove*" type="boolean" defaultValue="{{default}}">
    	<p>Whether to allow removing options or not, when set to true a remove button will appear on the option on hover</p>
    </PropSection>

    <PropSection title="variation*" type="string" defaultValue="{{default}}">
    	<p>The component variation style</p>
    	<p>Allowed options</p>
    	<List>
    		<span label={<Code small={true}>default</Code>}/>
    		<span label={<Code small={true}>icon-only</Code>}/>
    		<span label={<Code small={true}>compound</Code>}/>
    		<span label={<Code small={true}>image</Code>}/>
    	</List>
    </PropSection>

    <PropSection title="tag*" type="string" defaultValue="{{default}}">
    	<p> A text to appear on a custom tag </p>
    </PropSection>

    <PropSection title="tagColor*" type="string" defaultValue="{{default}}">
    	<p> The color variation for the tag </p>
    	<p> Allowed options </p>
    	<List>
    		<span label={<Code small={true}>default</Code>} />
    		<span label={<Code small={true}>blue</Code>} />
    		<span label={<Code small={true}>yellow</Code>} />
    		<span label={<Code small={true}>red</Code>} />
    		<span label={<Code small={true}>green</Code>} />
    		<span label={<Code small={true}>white</Code>} />
    		<span label={<Code small={true}>black</Code>} />
    		<span label={<Code small={true}>navy</Code>} />
    	</List>
    </PropSection>

    <PropSection title="imageUrl*" type="string" defaultValue="{{default}}">
    	<p> The url to the image to be displayed in the image variation </p>
    </PropSection>

    <PropSection title="onChange*" type="function" defaultValue="{{default}}">
    	<p>A callback function to be invoked upon radio input change, the function has the following parameter </p>
    	<List>
    		<span label={<><Code small={true}>isChecked</Code> The new checked value of the input </>}/>
    		<span label={<><Code small={true}>value</Code> The radio option value </>}/>
    		<span label={<><Code small={true}>name</Code> The name of the radio input </>}/>
    	</List>
    </PropSection>

    <PropSection title="onRemove*" type="function" defaultValue="{{default}}">
    	<p> A callback function to be invoked upon removing an option </p>
    </PropSection>

</Section>
