import { Section, Preview, Snippet, Code } from '@wpmudev/storybook';
import { Avatar } from '../../../lib/react-avatar';
import dedent from 'dedent';

export const SmallAvatarCode = dedent`
import React from 'react';\n
import { Avatar } from '@wpmudev/sui-react';\n
const Example() => {
	return <Avatar isSmall={true} status="confirmed" />;
}\n
export default Example;
`;

<Section
	title="Small"
	contained={true}
	style={{ paddingRight: 0, paddingBottom: 0, paddingLeft: 0 }}
>
	<p>
		Set the <Code small={true}>isSmall</Code> prop to reduce the avatar
		width and height, fitting with the design context.
	</p>
	<Preview wrapper={true}>
		<Avatar isSmall={true} status="confirmed" />
	</Preview>
	<Snippet language="js">{SmallAvatarCode}</Snippet>
</Section>
