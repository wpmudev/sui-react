import { useRef } from "react"
import { Section, Snippet } from "@wpmudev/sui-docs"
import { Modal, ModalHeader, ModalFooter, ModalBody } from "../../src"
import dedent from "dedent"
import { Button } from "@wpmudev/sui-button"
import { Input } from "@wpmudev/sui-input"
import { FormField } from "@wpmudev/sui-form-field"

export const simpleCode = dedent`

    export const SimpleModal = () => {

    const ref = useRef();

    return (
        <>
            <Modal id="simple-modal" ref={ref}>

                <ModalHeader title="Apply Config">
                    Choose the audience you want to send form data to.
                </ModalHeader>

                <ModalBody>
                    Are you sure you want to apply the Default config to this site? We
                    recommend you have a backup available as your existing settings
                    configuration will be overridden.
                </ModalBody>

                <ModalFooter>
                    <Button
                        type="secondary"
                        color="blue"
                        isSmall={true}
                        onClick={() => ref.current.closeModal()}
                    >
                       Cancel
                    </Button>

                    <Button
                        type="primary"
                        color="blue"
                        isSmall={true}
                        onClick={() => {
                            console.log("Submitted")
                        }}
                    >
                        Apply
                    </Button>
                </ModalFooter>
            </Modal>

            <Button
               type="primary"
               color="blue"
               onClick={() => {
    	          ref.current.openModal()
               }}
            >
                Apply Config
            </Button>
        </>
    )

}

`

export const SimpleModal = () => {

    const ref = useRef();

    return (
    	<>
    		<Modal id="simple-modal" ref={ref}>

    			<ModalHeader title="Apply Config">
    				Choose the audience you want to send form data to.
    			</ModalHeader>

    			<ModalBody>
    				Are you sure you want to apply the Default config to this site? We
    				recommend you have a backup available as your existing settings
    				configuration will be overridden.
    			</ModalBody>

    			<ModalFooter>
    				<Button
    					type="secondary"
    					color="blue"
    					isSmall={true}
    					onClick={() => ref.current.closeModal()}
    				>
    					Cancel
    				</Button>

    				<Button
    					type="primary"
    					color="blue"
    					isSmall={true}
    					onClick={() => {
    						console.log("Submitted")
    					}}
    				>
    					Apply
    				</Button>
    			</ModalFooter>
    		</Modal>

    			<Button
    				type="primary"
    				color="blue"
    				onClick={() => {
    					ref.current.openModal()
    				}}
    			>
    				Apply Config
    			</Button>
    	</>
    )

}

export const advancedCode = dedent`
    export const AdvancedModal = () => {

    const ref = useRef();

    return (
        <>
            <Modal id="advanced-modal" ref={ref}>

                <ModalHeader title="Submit Button" />

                <ModalBody>
                    <FormField label="Button Text">
                        <Input id="button-text" placeholder="Enter Text" />
                    </FormField>
                    <FormField label="Button Processing Text" helper="This text will appear as button text while the quiz is being submitted.">
                        <Input id="button-processing-text" placeholder="Sending..." />
                    </FormField>
                    <hr />
                    <FormField label="Custom CSS Class" helper='These will be output as you see them here. To add multiple classes, separate them with a space. For example, "form-submit-btn button" will add two classes "form-submit-btn" and "button".'>
                        <Input id="custom-css-class" placeholder="Eg., form-submit-btn" />
                    </FormField>
                </ModalBody>

                <ModalFooter>
                    <Button
                        type="tertiary"
                        color="black"
                        isSmall={true}
                        onClick={() => ref.current.closeModal()}
                    >
                        Tertiary
                    </Button>
                    <Button
                        type="secondary"
                        color="black"
                        isSmall={true}
                        onClick={() => ref.current.closeModal()}
                    >
                        Secondary
                    </Button>

                    <Button
                        type="primary"
                        color="blue"
                        isSmall={true}
                        onClick={() => {
                            console.log("Submitted")
                        }}
                    >
                        Primary
                    </Button>
                </ModalFooter>
            </Modal>

                <Button
                    type="primary"
                    color="blue"
                    onClick={() => {
                        ref.current.openModal()
                    }}
                >
                    Open form
                </Button>
        </>
    )

} `

export const AdvancedModal = () => {

    const ref = useRef();

    return (
        <>
            <Modal id="advanced-modal" ref={ref}>

                <ModalHeader title="Submit Button" />

                <ModalBody>
                    <FormField label="Button Text">
                        <Input id="button-text" placeholder="Enter Text" />
                    </FormField>
                    <FormField label="Button Processing Text" helper="This text will appear as button text while the quiz is being submitted.">
                        <Input id="button-processing-text" placeholder="Sending..." />
                    </FormField>
                    <hr />
                    <FormField label="Custom CSS Class" helper='These will be output as you see them here. To add multiple classes, separate them with a space. For example, "form-submit-btn button" will add two classes "form-submit-btn" and "button".'>
                        <Input id="custom-css-class" placeholder="Eg., form-submit-btn" />
                    </FormField>
                </ModalBody>

                <ModalFooter>
                    <Button
                        type="tertiary"
                        color="black"
                        isSmall={true}
                        onClick={() => ref.current.closeModal()}
                    >
                        Tertiary
                    </Button>
                    <Button
                        type="secondary"
                        color="black"
                        isSmall={true}
                        onClick={() => ref.current.closeModal()}
                    >
                        Secondary
                    </Button>

                    <Button
                        type="primary"
                        color="blue"
                        isSmall={true}
                        onClick={() => {
                            console.log("Submitted")
                        }}
                    >
                        Primary
                    </Button>
                </ModalFooter>
            </Modal>

                <Button
                    type="primary"
                    color="blue"
                    onClick={() => {
                        ref.current.openModal()
                    }}
                >
                    Open form
                </Button>
        </>
    )

}

<Section title="Simple Dialog" contained={true} style={{ paddingTop: 30 }}>
	<p>Simple dialogs are commonly used to display confirmation messages.</p>
	<div className="sui-wrap sui-theme--light" style={{ marginBottom: 30 }}>
		<SimpleModal />
	</div>
	<Snippet language="js">{simpleCode}</Snippet>
</Section>

<Section title="Advanced Dialog" contained={true} style={{ paddingTop: 30 }}>
	<p>
		Advanced dialogs are utilised when the body contains extensive content such
		as forms and other lengthy information.
	</p>
	<div className="sui-wrap sui-theme--light" style={{ marginBottom: 30 }}>
		<AdvancedModal />
	</div>
	<Snippet language="js">{advancedCode}</Snippet>
</Section>
