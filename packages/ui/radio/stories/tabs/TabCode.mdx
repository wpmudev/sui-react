import {
	 Section, 
	 Row,
	Col, 
	Table, 
	List, 
	Code, 
	Tag, 
	PackageInstallGuide, 
	PropSection, 
	CommonPropsNotice,
	FormValidationProps
} from "@wpmudev/sui-docs"

<PackageInstallGuide packageName="@wpmudev/sui-radio" />

<Section title="Props" border={true}>
  <PropSection title="id" type="string">
    <p>Unique identifier for the radio</p>
  </PropSection>

  <PropSection title="label" type="string" defaultValue="label">
    <p>The label text for the radio</p>
  </PropSection>

  <PropSection title="description" type="string">
    <p>Additional information about the radio</p>
  </PropSection>

  <PropSection title="value" type="string">
    <p>The value of the radio, This value represents the first parameter passed to the <Code small={true}>onChange</Code> function of the RadioGroup component discussted below</p>
  </PropSection>

  <PropSection title="tag" type="string">
    <p>A tag associated with the radio button, this will appear in the right side of the label</p>
  </PropSection>

  <PropSection title="isDisabled" type="boolean" defaultValue="false">
    <p>Whether to disable the radio or not</p>
  </PropSection>
</Section>

<Section title="RadioGroup" contained={true} style={{ paddingTop: 30, paddingBottom: 0 }}>
  <p>Always Wrap the Radio component within this component, it has some essential props to change the variation and interact with the Radio changes </p>
  <p>The RadioGroup component has the following props</p>

  <PropSection title="name" type="string">
    <p>
      The name for the RadioGroup
    </p>
  </PropSection>

  <PropSection title="isInline" type="boolean" defaultValue="true">
    <p>
      When set to <Code small={true}>true</Code> the radio elements will appear inline
    </p>
  </PropSection>

  <PropSection title="children" type="React Node">
    <p>
      The content of the RadioGroup
    </p>
  </PropSection>

  <PropSection title="onChange" type="function">
    <p>
      A callback function to be invoked upon the selection of one of the radio elements, the first parameter represents the <Code small={true}>value</Code> prop of the selected radio
    </p>
  </PropSection>

  <PropSection title="defaultValue" type="string">
    <p>
      Represnts the default selected radio value
    </p>
  </PropSection>

  <PropSection title="isSmall" type="boolean" defaultValue="false">
    <p>
      Set this to <Code small={true}>true</Code> if you want to display the group in a smaller size
    </p>
  </PropSection>

  <PropSection title="isDisabled" type="boolean" defaultValue="false">
    <p>
      Set this to <Code small={true}>true</Code> to make all the radio elements within the group non-interactive
    </p>
  </PropSection>

  <PropSection title="asBlock" type="boolean" defaultValue="false">
    <p>
      Whether to display each radio element as a block or not, a block appears as a standalone box with a border
    </p>
  </PropSection>

  <FormValidationProps />
</Section>

<CommonPropsNotice />
