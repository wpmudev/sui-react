import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection } from "@wpmudev/sui-docs"

<PackageInstallGuide packageName="@wpmudev/sui-config-table" />

<Section title="Props" border={true}>
	<PropSection title="hasCreatedDate" type="Boolean" defaultValue="false">
    	<p>Whether to show created date or not </p>
    </PropSection>

	<PropSection title="hasLastApplied" type="Boolean" defaultValue="false">
    	<p>Whether to show last applied date or not </p>
    </PropSection>

	<PropSection title="proItems" type="Array">
    	<p>The list of items, each item is an string which will be compared with the configs item and adds a pro tag </p>
    </PropSection>

    <PropSection title="configs" type="Array">
    	<p>The list of configs, each item is an object with custom information, check the examples tab for details </p>
    </PropSection>

    <PropSection title="onActionClick" type="function">
    	<p>A callback function to be invoked upon clicking an action, the parameters of this function are: </p>
    	<List>
    		<span label={<><Code small={true}>id</Code> The config record id </>} />
    		<span label={<><Code small={true}>type</Code> The type of the config record, it could be one of two values <Code small={true}>apply</Code> & <Code small={true}>configure</Code> </>} />
    	</List>
    </PropSection>

    <PropSection title="className" type="string">
    	<p>Custom CSS className(s) for further customization</p>
    </PropSection>

</Section>
