import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection, CommonPropsNotice, Snippet } from '@wpmudev/sui-docs';

<PackageInstallGuide packageName="@wpmudev/sui-tag" />

<Section title="Props" border={true}>
	<PropSection title="colorScheme" type="string">
		<p>The color of the tag</p>
		<p>Allowed options:</p>
		<List>
			<span label={<Code small={true}>blue</Code>} />
			<span label={<Code small={true}>yellow</Code>} />
			<span label={<Code small={true}>red</Code>} />
			<span label={<Code small={true}>green</Code>} />
			<span label={<Code small={true}>white</Code>} />
			<span label={<Code small={true}>black</Code>} />
		</List>
	</PropSection>

	<PropSection title="design" type="string">
		<p>The design of the tag</p>
		<p>Allowed options:</p>
		<List>
			<span label={<><Code small={true}>solid</Code> The tag will have a solid background</>} />
			<span label={<><Code small={true}>outlined</Code> The tag will have a transparent background</>} />
		</List>
	</PropSection>

	<PropSection title="isSmall" type="boolean" defaultValue="false">
		<p>When set to <Code small={true}> true </Code> the tag will have a smaller size</p>
	</PropSection>

	<PropSection title="isUppercase" type="boolean" defaultValue="false">
		<p>When set to <Code small={true}> true </Code>, the tag text will be in uppercase</p>
	</PropSection>

	<PropSection title="isDisabled" type="boolean" defaultValue="false">
		<p>When set to <Code small={true}> true </Code>, the tag  will be disabled and it will have a diffrent style</p>
	</PropSection>

	<PropSection title="isDismissable" type="boolean" defaultValue="false">
		<p>When set to <Code small={true}> true </Code>, the tag will have a dismiss button</p>
	</PropSection>

	<PropSection title="onDismiss" type="function">
		<p>A callback function to be invoked on the click of dismiss button</p>
	</PropSection>

	<PropSection title="className" type="string">
		<p>A custom className </p>
	</PropSection>

	<PropSection title="contentWrap" type="string" defaultValue="default">
		<p>When the available space is unsufficient, the tag could have one of three styles: </p>
		<List>
		<span label={<><Code small={true}>default</Code> for single line text. </>} />
			<span label={<><Code small={true}>multiline</Code> for a multilined text. </>} />
			<span label={<><Code small={true}>truncated</Code> for a truncated text. </>} />
		</List>
	</PropSection>

	<PropSection title="children" type="React Node">
		<p>The content of the tag</p>
	</PropSection>

</Section>

<Section title="Function to add tags in plugin menu." border={true}>
  <p>Use this function to use tags in plugin menu.</p>
  <List>
    <span label={"Replace plugin_id with the unique id of the plugin in the body tag and plugin_name in the class."} />
    <span label={<><Code small={true}>li:nth-child(3)</Code> the number should be the item number in the list which to target. </>} />
  </List>
<Snippet language="console" hideToggle={true}>
{`<?php
public function print_menu_styles() {
?>
<style>
#adminmenu #plugin_id li span.{plugin_name}-menu-tag {
  background-color: #737373;
  border-radius: 12px;
  color: #ffffff;
  font-size: 10px;
  font-style: normal;
  font-weight: 700;
  height: 16px;
  letter-spacing: -0.1px;
  line-height: 12px;
  padding: 2px 8px;
  text-align: center;
  width: 38px;
}

#adminmenu #plugin_id li span.{plugin_name}-menu-tag.{plugin_name}-menu-tag--green {
  background-color: #138639;
}

#adminmenu #plugin_id li span.{plugin_name}-menu-tag.{plugin_name}-menu-tag--blue {
  background-color: #0059ff;
}

#adminmenu #plugin_id li span.{plugin_name}-menu-tag.{plugin_name}-menu-tag--navy {
  background-color: #0045c4;
}

#adminmenu #plugin_id li span.{plugin_name}-menu-tag.{plugin_name}-menu-tag--yellow {
  background-color: #ffcc20;
  color: #1a1a1a;
}

#adminmenu #plugin_id li span.{plugin_name}-menu-tag.{plugin_name}-menu-tag--red {
  background-color: #f45c59;
}

#adminmenu #plugin_id li span.{plugin_name}-menu-tag.{plugin_name}-menu-tag--black {
  background-color: #1a1a1a;
}

#adminmenu #plugin_id li span.{plugin_name}-menu-tag.{plugin_name}-menu-tag--white {
  background-color: #ffffff;
  color: #1a1a1a;
}
</style>
<?php
}
?>`}
</Snippet>
<Snippet language="php" hideToggle={true}>
{`public static function get_menu_title() {
 return esc_html__( 'Reseller', 'thc' ) . '&nbsp;<span class="{plugin_name}-menu-tag {plugin_name}-menu-tag--green">' . esc_html__( 'NEW', 'plugin-name' ) . '</span>';
}`}
</Snippet>
</Section>

<CommonPropsNotice />