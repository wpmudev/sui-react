import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection } from "@wpmudev/sui-docs"

<PackageInstallGuide packageName="@wpmudev/sui-search" />

<Section title="Props" border={true}>
	<PropSection title="id" type="string" defaultValue="{{default}}">
		<p>Sets the search input id attribute</p>
	</PropSection>

    <PropSection title="variation" type="string" defaultValue="{{default}}">
    	<p>Sets the variation of the search input</p>
    	<p>Allowed options</p>
    	<List>
    		<span label="basic" />
    		<span label="smart" />
    	</List>
    </PropSection>

    <PropSection title="options" type="array" defaultValue="{{default}}">
    	<p>The available options for search in the smart variation</p>
    </PropSection>

    <PropSection title="defaultValue" type="string" defaultValue="{{default}}">
    	<p>Adds a custom default value to the search input</p>
    </PropSection>

    <PropSection title="onChange" type="function" defaultValue="{{default}}">
    	<p>Callback function to be invoked upon text change</p>
    </PropSection>

    <PropSection title="allowClear" type="boolean" defaultValue="{{default}}">
    	<p>Whether to display clear button or not</p>
    </PropSection>

    <PropSection title="isDisabled" type="boolean" defaultValue="{{default}}">
    	<p>When set to <Code small={true}>true</Code> the search input will be disable</p>
    </PropSection>

    <PropSection title="searchHint" type="string" defaultValue="{{default}}">
    	<p>Provides a descriptive hint for search</p>
    </PropSection>

    <PropSection title="searchMinChars" type="number" defaultValue="{{default}}">
    	<p>When set to <Code small={true}>true</Code> the search input will be disable</p>
    </PropSection>

    <PropSection title="className" type="string" defaultValue="{{default}}">
    	<p>Adds a custom css className(s) for further customization</p>
    </PropSection>

</Section>
