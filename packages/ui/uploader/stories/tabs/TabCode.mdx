import { Section, Code, PackageInstallGuide, PropSection, CommonPropsNotice } from "@wpmudev/sui-docs"

<PackageInstallGuide packageName="@wpmudev/sui-uploader" />

<Section title="Props" border={true}>

    	<PropSection title="btnTitle" type="string" defaultValue="Upload File">
    		<p>The label to appear in the upload button</p>
    	</PropSection>

    	<PropSection title="multiple" type="boolean" defaultValue="true">
    		<p>When set to <Code isSmall={true}> true </Code>, the component will allow for multiple files upload </p>
    	</PropSection>

    	<PropSection title="accept" type="boolean">
    		<p>The allowed <a href="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/file#accept" target="_blank"> file types</a> to upload.</p>
    	</PropSection>

    	<PropSection title="allowDragAndDrop" type="boolean" defaultValue="true">
    		<p>When set to <Code isSmall={true}> true </Code>, Users will be able to use drag and drop to upload files </p>
    	</PropSection>

    	<PropSection title="onChange" type="function">
    		<p>A callback function to be invoked when a file is uploaded, the first parameter is an <Code isSmall={true}> array </Code> of the uploaded files objects</p>
    	</PropSection>

</Section>

<CommonPropsNotice />
