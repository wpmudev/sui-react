import { Section, Code, PackageInstallGuide, PropSection } from "@wpmudev/sui-docs"

<PackageInstallGuide packageName="@wpmudev/sui-segmented-control" />

<Section title="Props" border={true}>

    	<PropSection title="name*" type="string" defaultValue="{{default}}">
    		<p>Name of the radio input elements.</p>
    		<p>Under the hood of the segmented control component, the options are input radio elements</p>
    	</PropSection>

    	<PropSection title="defaultValue" type="string" defaultValue="{{default}}">
    		<p>The default selected value.</p>
    	</PropSection>

    	<PropSection title="onChange" type="function" defaultValue="{{default}}">
    		<p>A callback function triggered when the value of the segmented control changes. the first parameter is the new selected value</p>
    	</PropSection>

    	<PropSection title="isFullWidth" type="boolean" defaultValue="{{default}}">
    		<p>A boolean indicating whether the segmented control should take full width</p>
    	</PropSection>

    	<PropSection title="children*" type="React Node" defaultValue="{{default}}">
    		<p>
    			The child elements to be rendered inside the segmented control these are the individual segments.
    		</p>
    	</PropSection>

</Section>

<Section
	title="Sub Components"
	contained={true}
	style={{ paddingTop: 30, paddingBottom: 0 }}
>

    <Section title="SegmentedControlButton" contained={true} style={{ paddingTop: 0, paddingLeft: 20, paddingRight: 0 }}>
    	<p>
    		This component represents an individual item or button for the segmented control component.
    	</p>

    	<PropSection title="value*" type="string" defaultValue="{{default}}">
    		<p>
    			The value associated with the button
    		</p>
    	</PropSection>

    	<PropSection title="icon" type="string" defaultValue="{{default}}">
    		<p>
    			An optional Custom icon for the button, you can check the available icons on [sui-icons](https://github.com/wpmudev/sui-icons) package.
    		</p>
    	</PropSection>

    	<PropSection title="ariaLabel" type="string" defaultValue="{{default}}">
    		<p>
    			Button label for accessibility purposes
    		</p>
    	</PropSection>

    	<PropSection title="isDisabled" type="boolean" defaultValue="{{default}}">
    		<p>
    			When set to <Code isSmall={true}>true</Code>, the button will be disabled
    		</p>
    	</PropSection>

    	<PropSection title="children" type="React Node" defaultValue="{{default}}">
    		<p>
    			The child elements to be rendered inside
    		</p>
    	</PropSection>
    </Section>

</Section>
