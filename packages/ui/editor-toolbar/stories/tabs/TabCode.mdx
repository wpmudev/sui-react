import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection } from "@wpmudev/sui-docs"

<PackageInstallGuide packageName="@wpmudev/sui-editor-toolbar" />

<Section title="Props" border={true}>
	<PropSection title="title" type="string" defaultValue="{{default}}">
		<p>The heading text for the Toolbar</p>
	</PropSection>

    <PropSection title="actions" type="Array" defaultValue="{{default}}">
    	<p>The action buttons to be displayed on the right side of the toolbar </p>
    </PropSection>

    <PropSection title="allowBack" type="boolean" defaultValue="{{default}}">
    	<p>Whether to display the back button or not </p>
    </PropSection>

    <PropSection title="onBackClick" type="function" defaultValue="{{default}}">
    	<p>A callback function to be invoked upon clicking the back button</p>
    </PropSection>

    <PropSection title="onSettingClick" type="function" defaultValue="{{default}}">
    	<p>A callback function to be invoked upon clicking the settings button</p>
    </PropSection>

    <PropSection title="children" type="React Node" defaultValue="{{default}}">
    	<p>The content to be placed right after the title</p>
    </PropSection>

    <PropSection title="alignCenter" type="boolean" defaultValue="{{default}}">
    	<p>Whether to center the content or keep it aligned to the right</p>
    </PropSection>

</Section>
