import { Meta, Canvas, Source } from '@storybook/addon-docs';
import LinkTo from '@storybook/addon-links/react';
import { Docs } from '@wpmudev/docs-elements';
import dedent from 'dedent';

export const sampleLog = () => {
	hrefTo('Layout', 'Grid').then(action('URL of this story'));
	return <span>See action logger</span>;
};

<Meta title="SUI/CSS Framework/Layout/Overview" />

<Docs.Page title="Container" light={true} status="draft">
	<Docs.Content
		title={
			<>
				Containers are the most basic layout and are{' '}
				<strong>required</strong> when using our default{' '}
				<LinkTo kind="sui-css-framework-layout-grid" story="grid">
					grid system
				</LinkTo>
				. Choose between full-width (meaning it's 100% wide all the time)
				and contained (meaning, it has a maximum width) content.
			</>
		}
	>
		<Docs.Section>
			<p>
				Use <code>sui-layout</code> class name to define the main
				container. It will span the entire width of the remaining space
				(without admin bar) on the WordPress admin page.
			</p>
			<Source
				dark
				language="html"
				code={dedent`
<div class="sui-layout">\n
	...\n
</div>
				`}
			/>
			<Docs.Section title="Modifier: Top Spacing" nested={true}>
				<p>
					Use <code>sui-layout--justify</code> modifier class name to
					add some spacing on top of the layout and separate the
					content from WordPress Admin top bar.
				</p>
				<Source
					dark
					language="html"
					code={dedent`
<div class="sui-layout sui-layout--justify">\n
	...\n
</div>
					`}
				/>
			</Docs.Section>
			<Docs.Section title="Modifier: Sides Spacing" nested={true}>
				<p>
					Use <code>sui-layout--spacing</code> modifier class name to
					add some padding on the sides of the layout. Have in mind
					for using the Top Navigation Bar is fine if you skip this
					class name but for other types of content it is a must-go
					modifier.
				</p>
				<Source
					dark
					language="html"
					code={dedent`
<div class="sui-layout sui-layout--spacing">\n
	...\n
</div>
					`}
				/>
			</Docs.Section>
		</Docs.Section>
		<Docs.Section title="Layout Content">
			<p>
				Use <code>sui-layout__content</code> class name for an inner div
				that wraps the content of your plugin. This wrapper will set a
				maximum width and vertically align it to the center of the
				remaining space (wihtout admin bar) on the WordPRESS admin page.
			</p>
			<Source
				dark
				language="html"
				code={dedent`
<div class="sui-layout">\n
	<div className="sui-layout__content">\n
		...\n
	</div>\n
</div>
				`}
			/>
		</Docs.Section>
	</Docs.Content>
	<Docs.Footer />
</Docs.Page>
