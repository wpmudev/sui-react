import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection } from "@wpmudev/sui-docs"

<PackageInstallGuide packageName="@wpmudev/sui-password-field" />

<Section title="Props" border={true}>

    	<PropSection title="id*" type="string" defaultValue="{{default}}">
    		<p>A unique identifier for the password field</p>
    	</PropSection>

    	<PropSection title="placholder*" type="string" defaultValue="{{default}}">
    		<p>A placholder for the password field</p>
    	</PropSection>

    	<PropSection title="button*" type="object" defaultValue="{{default}}">
    		<p> Specifies the button type for showing and hiding the password. </p>
    		<p> button is an object that has a single property: <Code small={true}>type</Code> </p>
    		<p> Allowed values for the type property: </p>
    		<List>
    			<span label={<><Code small={true}>icon-button</Code> displays an icon button </>} />
    			<span label={<><Code small={true}>standard</Code> displays a text button instead ( show & hide ) </>} />
    		</List>
    	</PropSection>

    	<PropSection title="isSmall" type="boolean" defaultValue="{{default}}">
    		<p> When set to <Code small={true}> true </Code> the field will be smaller </p>
    	</PropSection>

    	<PropSection title="isDisabled*" type="boolean" defaultValue="{{default}}">
    		<p> Indicates whether the field is disabled. </p>
    	</PropSection>

</Section>
