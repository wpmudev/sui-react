import { Section, Snippet, Code } from '@wpmudev/storybook';
import dedent from 'dedent';

export const StandardButtonCode = dedent`
// from named entry point (to ensure minimum code is brought in)
import { Button } from '@wpmudev/button/standard-button';\n
// from main entry point
import { Button } from '@wpmudev/sui-react';
`;

export const LoadingButtonCode = dedent`
// from named entry point (to ensure minimum code is brought in)
import { LoadingButton } from '@wpmudev/button/loading-button';\n
// from main entry point
import { LoadingButton } from '@wpmudev/sui-react';
`;

export const ToggleButtonCode = dedent`
// from named entry point (to ensure minimum code is brought in)
import { ToggleButton } from '@wpmudev/button/toggle-button';\n
// from main entry point
import { ToggleButton } from '@wpmudev/sui-react';
`;

<Section title="Button (standard button)" contained={true} border={true} style={{ paddingRight: 0, paddingLeft: 0 }}>
	<p>The most commonly used button component. This button has different appearances, as well as supported the existing dark mode solution.</p>
	<p>It does not support the discouraged theming approach.</p>
	<Snippet language="js">{StandardButtonCode}</Snippet>
</Section>

<Section title="Loading button" contained={true} border={true} style={{ paddingRight: 0, paddingLeft: 0 }}>
	<p>A small wrapper around Button that allows you to show an @atlaskit/spinner as an overlay on the button when you set an isLoading prop to true.</p>
	<p>LoadingButton takes care of applying a spinner with the correct size and color based on the button spacing and appearance props.</p>
	<Snippet language="js">{LoadingButtonCode}</Snippet>
</Section>

<Section title="Toggle button" contained={true} style={{ paddingRight: 0, paddingBottom: 0, paddingLeft: 0 }}>
	<p>A small wrapper around Button that allows you to show an @atlaskit/spinner as an overlay on the button when you set an isLoading prop to true.</p>
	<p>LoadingButton takes care of applying a spinner with the correct size and color based on the button spacing and appearance props.</p>
	<Snippet language="js">{ToggleButtonCode}</Snippet>
</Section>
