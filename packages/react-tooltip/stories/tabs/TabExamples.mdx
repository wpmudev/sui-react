import { Section, Switcher, Preview, Snippet } from '@wpmudev/storybook';
import { Tooltip } from '../../src';
import dedent from 'dedent';

export const sampleCode = dedent`
import React from 'react';\n
import { Tooltip } from '@wpmudev/sui-react';\n
const Example() => {
	return <Tooltip label="Button" appearance="primary" color="black">Button default</Tooltip>;
}\n
export default Example;
`;

export const overflowSampleCode = dedent`
import React from 'react';\n
import { Tooltip } from '@wpmudev/sui-react';\n
const Example() => {
	return <Tooltip label="Button" appearance="primary" color="black" customWidth={200}>When the content is long, it will be wrapped.</Tooltip>;
}\n
export default Example;
`;

<Section contained={true} style={{ paddingTop: 0 }}>
	<p>
		Below is the default form of a button, used for most cases; it is not
		impactful enough to represent the primary action in a container but
		necessary in some scenarios.
	</p>
	<Preview wrapper={true}>
		<div style={{ padding: '40px' }}>
			<Tooltip
				label="Tooltip"
				appearance="primary"
				position="top"
				color="black"
			>
				Default tooltip
			</Tooltip>
		</div>
	</Preview>
	<Snippet language="js">{sampleCode}</Snippet>
</Section>

<Section title="Overflow" contained={true} style={{ paddingTop: 0 }}>
	<p>If the tooltip content exceeds the maximum width, it will be wrapped.</p>
	<Preview wrapper={true}>
		<div style={{ padding: '40px' }}>
			<Tooltip
				label="Overflow"
				appearance="primary"
				position="right"
				color="black"
				customWidth={200}
			>
				When the content is long, it will be wrapped.
			</Tooltip>
		</div>
	</Preview>
	<Snippet language="js">{overflowSampleCode}</Snippet>
</Section>
