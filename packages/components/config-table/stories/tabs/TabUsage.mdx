import { Section, Row, Col } from "@wpmudev/storybook"

import Example from "../images/example.svg"
import States from "../images/states.svg"

<Section title="Example" style={{ paddingTop: 20 }}>
	<img src={Example} alt="Table Config Example" style={{ marginBottom: 20 }} />
	<h3 style={{marginTop: 0}}>1. Accordion header</h3>
    <p>Accordion header contains the section title and chevron icon that expands and collapses accordion content.</p>

    <h3>1. Config title</h3>
    <p>Config title will help users to recognize the config.</p>

    <h3>2. Date created</h3>
    <p>There will be the date that the config was created.</p>

    <h3>3. Last applied on</h3>
    <p>The last applied on date allows users to easily determine if the config has been applied or not. If the config has been applied, a date will be displayed when the config was last applied. If not,  there will be displayed “Never“.</p>

    <h3>4. Apply</h3>
    <p>The config can be applied at any time. If the config was already applied, it will also be utilized to update it.</p>

    <h3>5. Configure</h3>
    <p>Users can configure the config with the helpof the action button.</p>

    <h3>6. Open and collapse config</h3>
    <p>Users can view the full details of the config.</p>

</Section>

<Section title="States" border={true}>
	<img src={States} alt="Config Table States" />
</Section>
