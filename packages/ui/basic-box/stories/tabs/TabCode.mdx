import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection } from "@wpmudev/sui-docs"

<PackageInstallGuide packageName="@wpmudev/sui-basic-box" />

<Section title="Props" border={true}>
	<PropSection title="title" type="React Node" defaultValue="{{default}}">
		<p>Represents the heading text of the Box</p>
	</PropSection>

    <PropSection title="description" type="React Node" defaultValue="{{default}}">
    	<p>Additional explanation text that accompanies the title</p>
    </PropSection>

    <PropSection title="headerActions" type="React Node" defaultValue="{{default}}">
    	<p>Optional header actions to be displayed in the Header of the Box</p>
    </PropSection>

    <PropSection title="footerActions" type="React Node" defaultValue="{{default}}">
    	<p>Optional footer actions to be displayed in the footer</p>
    </PropSection>

    <PropSection title="children" type="React Node" defaultValue="{{default}}">
    	<p>Represents the body content of the Box</p>
    </PropSection>

    <PropSection title="className" type="React Node" defaultValue="{{default}}">
    	<p>Custom css className(s) for further customization</p>
    </PropSection>

    <PropSection title="isProp" type="React Node" defaultValue="{{default}}">
    	<p>Sets the type of the Box, the pro version have a slightly different background</p>
    </PropSection>

</Section>
