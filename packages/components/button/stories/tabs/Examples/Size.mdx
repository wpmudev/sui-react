import { Section, Preview, Snippet, Code } from '@wpmudev/storybook';
import { Button } from '../../../src';
import dedent from 'dedent';

export const SmallButtonCode = dedent`
import React from 'react';\n
import { Button } from '@wpmudev/sui-react';\n
const Example() => {
	return <Button size="sm">Button</Button>;
}\n
export default Example;
`;

<Section
	title="Small"
	contained={true}
	style={{ paddingRight: 0, paddingBottom: 0, paddingLeft: 0 }}
>
	<p>
		use the <Code small={true}>isSmall</Code> prop, to make the button smaller.
	</p>
	<Preview wrapper={true}>
		<Button isSmall="sm">Small Button</Button>
	</Preview>
	<Snippet language="js">{SmallButtonCode}</Snippet>
</Section>
