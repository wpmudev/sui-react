import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection } from "@wpmudev/sui-docs"

<PackageInstallGuide packageName="@wpmudev/sui-empty-state" />

<Section title="Props" border={true}>
	<PropSection title="logo" type="string | React Element " defaultValue="{{default}}">
		<p> The logo to be displayed, it could be a string or a React element like an SVG </p>
	</PropSection>

    <PropSection title="children" type="ReactNode" defaultValue="{{default}}">
    	<p> The content of the component, it could be any React element </p>
    </PropSection>

    <PropSection title="className" type="ReactNode" defaultValue="{{default}}">
    	<p> Custom css className(s) </p>
    </PropSection>

</Section>
