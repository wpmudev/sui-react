import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection } from "@wpmudev/sui-docs"

<PackageInstallGuide packageName="@wpmudev/sui-pagination" />

<Section title="Props" border={true}>
	<PropSection title="limit*" type="number" defaultValue="{{default}}">
		<p>Number of items per page</p>
	</PropSection>

    <PropSection title="numberOfItems" type="number" defaultValue="{{default}}">
    	<p>Number of total items</p>
    </PropSection>

    <PropSection title="previousLabel" type="string" defaultValue="{{default}}">
    	<p>The screen reader text for the previous button</p>
    </PropSection>

     <PropSection title="nextLabel" type="string" defaultValue="{{default}}">
    	<p>The screen reader text for the next button</p>
    </PropSection>

    <PropSection title="skip*" type="string" defaultValue="{{default}}">
    	<p>Whether to allow for skip navigation or not</p>
    </PropSection>

    <PropSection title="onChange" type="function" defaultValue="{{default}}">
    	<p>A callback function to be invoked when the page gets changed, the first parameter represents the new selected page </p>
    </PropSection>

</Section>
