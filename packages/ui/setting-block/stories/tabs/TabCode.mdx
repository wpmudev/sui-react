import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection } from "@wpmudev/sui-docs"

<PackageInstallGuide packageName="@wpmudev/sui-setting-block" />

<Section title="Props" border={true}>
	<PropSection title="title*" type="React Node" defaultValue="{{default}}">
		<p>The Heading text of the Setting</p>
	</PropSection>

    <PropSection title="description" type="React Node" defaultValue="{{default}}">
    	<p>An explanatory text that accompanies the title</p>
    </PropSection>

     <PropSection title="isPro" type="React Node" defaultValue="{{default}}">
    	<p>Changes the variation to pro, when set to <Code small={true}>true</Code> a pro tag will appear and the background slightly change</p>
    </PropSection>

    <PropSection title="children" type="React Node" defaultValue="{{default}}">
    	<p>The main content that appear in the right side of the block</p>
    </PropSection>

     <PropSection title="className" type="string" defaultValue="{{default}}">
    	<p>Custom css className(s) for further styling</p>
    </PropSection>

</Section>
