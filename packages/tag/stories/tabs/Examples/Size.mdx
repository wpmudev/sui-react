import { Section, Preview, Snippet, Code } from '@wpmudev/storybook';
import { Tag } from '../../../src';
import dedent from 'dedent';

export const SmallTagCode = dedent`
import React from 'react';\n
import { Tag } from '@wpmudev/sui-react';\n
const Example() => {
	return <Tag isSmall={true}>Tag</Tag>;
}\n
export default Example;
`;

<Section
	title="Small"
	contained={true}
	style={{ paddingRight: 0, paddingBottom: 0, paddingLeft: 0 }}
>
	<p>
		Set the <Code small={true}>isSmall</Code> prop to reduce the tag width
		and height, fitting with the design context.
	</p>
	<Preview wrapper={true}>
		<Tag isSmall={true}>Default tag</Tag>
	</Preview>
	<Snippet language="js">{SmallTagCode}</Snippet>
</Section>
