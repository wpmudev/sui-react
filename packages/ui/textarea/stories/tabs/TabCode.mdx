import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection } from '@wpmudev/sui-docs';

<PackageInstallGuide packageName="@wpmudev/sui-textarea" />

<Section title="Props" border={true}>
	<p>Textarea component have the following props.</p>
	<PropSection title="id" type="string" defaultValue="{{default}}">
		<p>Add a unique id to the textarea.</p>
	</PropSection>
	<PropSection title="className" type="string" defaultValue="{{default}}">
		<p>Add a class name to the textarea.</p>
	</PropSection>
	<PropSection title="value" type="string" defaultValue="{{default}}">
		<p>Reflects the value of the input field.</p>
	</PropSection>
	<PropSection title="description" type="string" defaultValue="{{default}}">
		<p>Reflects the description of the input field.</p>
	</PropSection>
	<PropSection title="descriptionId" type="string" defaultValue="{{default}}">
		<p>
			It helps screen reader to announce the description message of the
			field.
		</p>
	</PropSection>
	<PropSection title="errorMessage" type="string" defaultValue="{{default}}">
		<p>Reflects the error message of the input field.</p>
	</PropSection>
	<PropSection title="errorId" type="string" defaultValue="{{default}}">
		<p>
			It helps screen reader to announce the error message of the field.
		</p>
	</PropSection>
	<PropSection title="isSmall" type="boolean" defaultValue="{{default}}">
		<p>Set if the input is small.</p>
	</PropSection>
	<PropSection title="isDisabled" type="boolean" defaultValue="{{default}}">
		<p>Set if the input is disabled.</p>
	</PropSection>
</Section>
