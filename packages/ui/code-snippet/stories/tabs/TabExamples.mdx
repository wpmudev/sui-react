import { Section, Switcher, Preview, Snippet, Code } from "@wpmudev/sui-docs"
import { CodeSnippet } from "../../src"
import dedent from "dedent"

export const cssSnippet = `
h1 {
    color: blue;
    font-size: 24px;
}

`

export const htmlSnippet = `
    <!DOCTYPE html>
    <html>
    <head>
        <title>My Simple Webpage</title>
    </head>
    <body>
        <h1>Welcome to My Webpage</h1>
        <p>This is a simple example of an HTML webpage.</p>
    </body>
    </html>
`

export const sampleCode = dedent`
<CodeSnippet type="markup">${htmlSnippet}</CodeSnippet>
`

export const cssCode = dedent`
<CodeSnippet type="markup">${cssSnippet}</CodeSnippet>
`

<Section contained={true} style={{ paddingTop: 0 }}>
  <p>The default form of the code snippet.</p>

  <Preview wrapper={true}>
    <CodeSnippet>
      {htmlSnippet}
    </CodeSnippet>
  </Preview>

  <Snippet language="js">
    {sampleCode}
  </Snippet>
</Section>

<Section title="Languages" contained={true} style={{ paddingTop: 0 }}>
  <p>
    Use the language prop to change the programming language for a correct
    highlighting
  </p>

  <p>
    You can choose between <Code small={true}>markup</Code>,{" "}
    <Code small={true}>javascript</Code> and <Code small={true}>css</Code>
  </p>

  <Preview wrapper={true}>
    <CodeSnippet language="css">
      {cssSnippet}
    </CodeSnippet>
  </Preview>

  <Snippet language="js">
    {sampleCode}
  </Snippet>
</Section>

<Section title="Hide Copy" contained={true} style={{ paddingTop: 0 }}>
  <p>
    Use the prop <Code small={true}>copy=\{false}</Code> to remove the copy button
  </p>

  <Preview wrapper={true}>
    <CodeSnippet copy={false}>
      {htmlSnippet}
    </CodeSnippet>
  </Preview>

  <Snippet language="js">
    {sampleCode}
  </Snippet>
</Section>
