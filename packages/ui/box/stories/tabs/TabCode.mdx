import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection } from "@wpmudev/sui-docs"

<PackageInstallGuide packageName="@wpmudev/sui-box" />

<Section title="Box Props" border={true}>
    	<PropSection title="title" type="string" defaultValue="{{default}}">
    		<p>The title of the box.</p>
    	</PropSection>

    	<PropSection title="icon" type="string" defaultValue="{{default}}">
    		<p>Hide icon on the mobile view.</p>
    	</PropSection>

    	<PropSection title="hideMobileIcon" type="boolean" defaultValue="{{default}}">
    		<p>Hide icon on the mobile view.</p>
    	</PropSection>

    	<PropSection title="headerLeft" type="ReactNode | string" defaultValue="{{default}}">
    		<p>The content for the left header section of the box.</p>
    	</PropSection>

    	<PropSection title="headerRight" type="React Node | string" defaultValue="{{default}}">
    		<p>The content for the right header section of the box.</p>
    	</PropSection>

    	<PropSection title="style" type="object" defaultValue="{{default}}">
    		<p>Box inline styles </p>
    	</PropSection>

    	<PropSection title="isSmall" type="boolean" defaultValue="{{default}}">
    		<p>Make box small.</p>
    	</PropSection>

    	<PropSection title="className" type="string" defaultValue="{{default}}">
    		<p>Custom css class Name(s) </p>
    	</PropSection>

    	<PropSection title="children" type="React Node" defaultValue="{{default}}">
    		<p>The content of the field, it could be any form element </p>
    	</PropSection>

</Section>


<Section title="BoxGroup Props" border={true}>
	<PropSection title="isInline" type="boolean" defaultValue="{{default}}">
		<p>Whether the boxes should be displayed inline or not.</p>
	</PropSection>

	<PropSection title="isHeader" type="boolean" defaultValue="{{default}}">
		<p>Whether the boxes should be displayed as box header.</p>
	</PropSection>

	<PropSection title="isFooter" type="boolean" defaultValue="{{default}}">
		<p>Whether the boxes should be displayed as footer.</p>
	</PropSection>

	<PropSection title="style" type="object" defaultValue="{{default}}">
		<p>Box inline styles </p>
	</PropSection>

	<PropSection title="children" type="React Node" defaultValue="{{default}}">
		<p>The content of the field, it could be any form element </p>
	</PropSection>

</Section>
