import { Meta } from '@storybook/addon-docs';
import { Page, Section, PropSection, List, Code, Snippet, Message } from "@wpmudev/sui-docs";
import { Unstyled } from '@storybook/blocks';
import dedent from "dedent"

<Meta title="SUI/Common Props" />

export const StyleCode = dedent`
	<Box 
    _style={
        {
            backgroundColor: { 
                default: "purple",
                sm: "red",
                md: "blue",
                lg: "green",
                xl: "yellow"
            }
            mt: "10px";
        }
    }>
        Box Content
    </Box>

    /* 
    - Sets margin top to 10px for all screens

    - Sets a different background color for each screen as follows:
        “purple”: to the default selector
        “red”: max-width: 600px
        “blue”: min-width: 601px and max-width: 1024px
        “green”: min-width: 1025px and max-width: 1200px
        “yellow”: min-width: 1201px and max-width: 1600px
        
    */
`

export const htmlCode = dedent`
    <Button 
    icon="Close"
    _htmlProps=
            {
                {
                    "aria-label": "Close", 
                    "data-id": "close-1"
                }
            }
    /> 
`

export const shorthands = dedent`
// paddings
p: "padding",
px: "padding left / right",
py: "padding top / bottom",
pt: "paddingTop",
pr: "paddingRight",
pb: "paddingBottom",
pl: "paddingLeft",

// margins
m: "margin",
mx: "margin left / right",
my: "margin top / bottom",
mt: "marginTop",
mr: "marginRight",
mb: "marginBottom",
ml: "marginLeft",
`

<Unstyled>
	<Page.Simple
		title="Common Props"
		subtitle="The subsequent document outlines the shared common properties across all components within SUI library."
	>

        <Section
			contained={true}
			style={{ paddingTop: 0, paddingBottom: 30 }}
		>
			<Message color="yellow">
				Consider using these props only when it's necessary, 
                always prioritize using the component's own props for customization unless they don't offer what you need.  
			</Message>
		</Section>
		<Section
			contained={ true }
			border={ true }
			style={{ paddingTop: 0 }}
        >
            <PropSection title="_style" type="object">
                <p>
                    The <Code isSmall={true}>_style</Code> prop enables inline styling for the component, 
                    it accepts an object form similar to standard <a href="https://legacy.reactjs.org/docs/dom-elements.html#style" target="_blank">React inline styles</a>,
                    with three key  differences: 
                    
                    1. It doesn't append style attribute to the DOM, instead, it automatically generates a unique className for the styles. 
                    2. The possibility of adding values for different breakpoints.
                    3. It provides shorthands for margin and padding properties for easier use.   
                </p>
                
                <Snippet hideToggle={true}>{StyleCode}</Snippet>
                
                <h4>Shorthands</h4>
                <Snippet hideToggle={true}>{shorthands}</Snippet>
            </PropSection>

            
        </Section>
		<Section
			contained={ true }
			border={ true }
			style={{ paddingTop: 30 }}
        >
            <PropSection title="_htmlProps" type="object">
                <p>
                    Use <Code isSmall={true}>_htmlProps</Code> to pass any valid HTML attributes to the component, 
                    structured as an object, Note that attributes should retain their original casing, unlike the camelcased 
                    convention used in the <Code isSmall={true}>_style</Code> prop.
                </p>
                <Snippet hideToggle={true}>{htmlCode}</Snippet>
            </PropSection>
        </Section>
	</Page.Simple>
</Unstyled>
