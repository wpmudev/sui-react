import { Section, Preview, Snippet, Code } from "@wpmudev/storybook"
import { Recipient } from "../../../src"
import dedent from "dedent"

export const ConfirmedRecipientCode = dedent`
    <Recipient
        status="confirmed"
        userName="Recipient name" 
        userEmail="email@example.com" 
    />
`

export const NotAcceptedCode = dedent`
    <Recipient
        status="not-accepted"
        userName="Recipient name" 
        userEmail="email@example.com" 
    />
`

export const NotConnectedCode = dedent`
    <Recipient
        status="not-connected"
        userName="Recipient name" 
        userEmail="email@example.com" 
    />
`

export const InvitedRecipientCode = dedent`
    <Recipient 
        isInvited={true} 
        userName="Recipient name" 
        userEmail="email@example.com" 
    />
`

<Section
	title="Primary"
	contained={true}
	border={true}
	style={{ paddingRight: 0, paddingLeft: 0 }}
>
	<p>Primary is the default of the recipient</p>
	<Preview wrapper={true}>
		<Recipient
			appearance="primary"
			userName="Recipient name"
			userEmail="email@example.com"
		/>
	</Preview>
	<Snippet language="js">{ConfirmedRecipientCode}</Snippet>
</Section>

<Section
	title="Secondary"
	contained={true}
	border={true}
	style={{ paddingRight: 0, paddingLeft: 0 }}
>
	<p>There's also a secondary theme</p>
	<Preview wrapper={true}>
		<Recipient
			appearance="secondary"
			userName="Recipient name"
			userEmail="email@example.com"
		/>
	</Preview>
	<Snippet language="js">{ConfirmedRecipientCode}</Snippet>
</Section>
