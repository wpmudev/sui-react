import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection } from '@wpmudev/sui-docs';

<PackageInstallGuide packageName="@wpmudev/sui-input" />

<Section title="Props" border={true}>
	<p>Input component have the following props.</p>
	<PropSection title="label" type="string" defaultValue="{{default}}">
		<p>Reflects the label of the input field.</p>
	</PropSection>
	<PropSection title="id" type="string" defaultValue="{{default}}">
		<p>Add a unique id to the input.</p>
	</PropSection>
	<PropSection title="className" type="string" defaultValue="{{default}}">
		<p>Add a class name to the input.</p>
	</PropSection>
	<PropSection title="value" type="string" defaultValue="{{default}}">
		<p>Reflects the value of the input field.</p>
	</PropSection>
	<PropSection title="type" type="string" defaultValue="{{default}}">
		<p>
			Reflects the type of the input field whether it's text, number etc.
		</p>
	</PropSection>
	<PropSection title="isError" type="string" defaultValue="{{default}}">
		<p>Reflects the error status in the field.</p>
	</PropSection>
	<PropSection title="isSmall" type="boolean" defaultValue="{{default}}">
		<p>Set if the input is small.</p>
	</PropSection>
	<PropSection title="isDisabled" type="boolean" defaultValue="{{default}}">
		<p>Set if the input is disabled.</p>
	</PropSection>
	<PropSection title="iconLead" type="string" defaultValue="{{default}}">
		<p>Places an icon within the input, before the input's text.</p>
	</PropSection>
	<PropSection title="iconTrail" type="string" defaultValue="{{default}}">
		<p>Places an icon within the input, after the input's text.</p>
	</PropSection>
</Section>
