import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection } from '@wpmudev/sui-docs';

<PackageInstallGuide packageName="@wpmudev/sui-avatar" />

<Section title="Props" border={true}>
	<p>Avatar component have the following props.</p>
	<PropSection title="status*" type="string" defaultValue="{{default}}">
		<p>Reflects the status of the avatar.</p>
		<p>Allowed options:</p>
		<List>
			<span
				label={
					<>
						<Code small={true}>confirmed</Code>
					</>
				}
			/>
			<span
				label={
					<>
						<Code small={true}>awaiting</Code>
					</>
				}
			/>
			<span
				label={
					<>
						<Code small={true}>not-connected</Code>
					</>
				}
			/>
			<span
				label={
					<>
						<Code small={true}>not-accepted</Code>
					</>
				}
			/>
		</List>
	</PropSection>
	<PropSection title="isSmall*" type="string" defaultValue="{{default}}">
		<p>When set to <Code small={true}>true</Code> the avatar will be smaller</p>
	</PropSection>
	<PropSection title="image*" type="object" defaultValue="{{default}}">
		<p>Reflects the profile image of the avatar. the object accepts two properties</p>
		<List>
			<span label={<><Code>src</Code> The image link</>} />
			<span label={<><Code>alt</Code> The image alternative text</>} />
		</List>
	</PropSection>
	<PropSection title="className*" type="string" defaultValue="{{default}}">
		<p>A custom className(s) for the avatar</p>
	</PropSection>
</Section>
