import { Section, Switcher, Preview, Snippet, Code } from "@wpmudev/storybook"
import {
	BasicPagination,
	BasicExampleCode,
	SkipButtonsExample,
	SkipButtonsCode,
} from "./Examples/Examples"
import dedent from "dedent"

<Section contained={true} style={{ paddingTop: 30 }}>
	<p>Below is a general example of using pagination</p>
	<Preview wrapper={true}>
		<BasicPagination />
	</Preview>
	<Snippet language="js">{BasicExampleCode}</Snippet>
</Section>

<Section title="Skip Buttons" contained={true} style={{ paddingTop: 30 }}>
	<p>Use the prop <Code small={true}>skip=\{true\}</Code> to allow for skip navigation</p>
	<Preview wrapper={true}>
		<SkipButtonsExample />
	</Preview>
	<Snippet language="js">{SkipButtonsCode}</Snippet>
</Section>
