import { Section, Preview, Snippet, Code } from '@wpmudev/storybook';
import { Button } from '../../../lib/react-button';
import dedent from 'dedent';

export const LeadButtonCode = dedent`
import React from 'react';\n
import { Button } from '@wpmudev/sui-react';\n
const Example() => {
	return <Button iconLead="logo">Icon before</Button>;
}\n
export default Example;
`;

export const TrailButtonCode = dedent`
import React from 'react';\n
import { Button } from '@wpmudev/sui-react';\n
const Example() => {
	return <Button iconTrail="logo">Icon after</Button>;
}\n
export default Example;
`;

<Section title="Leading" contained={true} border={true} style={{ paddingRight: 0, paddingLeft: 0 }}>
	<p>Display an icon before the text.</p>
	<Preview wrapper={true}>
		<Button leadIcon="logo">Icon before</Button>
	</Preview>
	<Snippet language="js">{LeadButtonCode}</Snippet>
</Section>

<Section title="Trailing" contained={true} style={{ paddingRight: 0, paddingBottom: 0, paddingLeft: 0 }}>
	<p>Display an icon after the text.</p>
	<Preview wrapper={true}>
		<Button trailIcon="logo">Icon after</Button>
	</Preview>
	<Snippet language="js">{TrailButtonCode}</Snippet>
</Section>
