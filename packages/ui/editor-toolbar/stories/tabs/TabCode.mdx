import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection } from "@wpmudev/sui-docs"

<PackageInstallGuide packageName="@wpmudev/sui-editor-toolbar" />

<Section title="Props" border={true}>
	<PropSection title="title" type="string" defaultValue="Toolbar Title">
		<p>The heading text for the Toolbar</p>
	</PropSection>

    <PropSection title="actions" type="function">
    	<p> A function where the first parameter is the setting button element, it should return an array of React Nodes </p>
    </PropSection>

    <PropSection title="allowBack" type="boolean" defaultValue="true">
    	<p>Whether to display the back button or not </p>
    </PropSection>

    <PropSection title="onBackClick" type="function">
    	<p>A callback function to be invoked upon clicking the back button</p>
    </PropSection>

    <PropSection title="onSettingClick" type="function">
    	<p>A callback function to be invoked upon clicking the settings button</p>
    </PropSection>

    <PropSection title="children" type="React Node">
    	<p>The content to be placed right after the title</p>
    </PropSection>

    <PropSection title="alignCenter" type="boolean" defaultValue="false">
    	<p>Whether to center the content or keep it aligned to the right</p>
    </PropSection>

    <PropSection title="disableSetting" type="boolean" defaultValue="false">
    	<p>Whether to disbaled the settings or not</p>
    </PropSection>

     <PropSection title="disableBack" type="boolean" defaultValue="false">
    	<p>Whether to disable the back button or not</p>
    </PropSection>

</Section>
