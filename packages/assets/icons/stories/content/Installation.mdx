import { Section, Code, Snippet, NodeJSWarning } from "@wpmudev/sui-docs"

<Section contained={ true } style={{ paddingTop: 0 }}>
  <NodeJSWarning />
</Section>

<Section
  title={{
		content: "Install using NPM",
		small: true,
	}}
  contained={ true }
  style={{ paddingTop: 0 }}
>
  <p>In your project root directory (where the <Code>package.json</Code> file lives), run the following command:</p>

  ```js
  npm i @wpmudev/sui-icons --save-dev
  ```
</Section>

<Section
  title={{
		content: "Install using Yarn",
		small: true,
	}}
  contained={ true }
  style={{ paddingTop: 0 }}
>
  <p>In your project root directory (where the <Code>package.json</Code> file lives), run the following command:</p>

  ```js
  yarn add @wpmudev/sui-icons --dev
  ```
</Section>
