import { Section, Preview, Snippet, Code } from '@wpmudev/storybook';
import { Button } from '../../../lib/react-button';
import dedent from 'dedent';

export const PrimaryButtonCode = dedent`
import React from 'react';\n
import { Button } from '@wpmudev/sui-react/button';\n
const Example() => {
	return <Button theme="primary">Primary button</Button>;
}\n
export default Example;
`;

export const SecondaryButtonCode = dedent`
import React from 'react';\n
import { Button } from '@wpmudev/sui-react/button';\n
const Example() => {
	return <Button theme="secondary">Secondary button</Button>;
}\n
export default Example;
`;

export const TertiaryButtonCode = dedent`
import React from 'react';\n
import { Button } from '@wpmudev/sui-react/button';\n
const Example() => {
	return <Button theme="tertiary">Tertiary button</Button>;
}\n
export default Example;
`;

<Section title="Primary" contained={true} border={true} style={{ paddingRight: 0, paddingBottom: 0, paddingLeft: 0 }}>
	<p>Use a <Code>primary</Code> button to call attention to an action. Combined with <Code>blue</Code> color highlights a strongest call to action on a page.</p>
	<Preview wrapper={true}>
		<Button theme="primary">Primary button</Button>
	</Preview>
	<Snippet language="js">{PrimaryButtonCode}</Snippet>
</Section>

<Section title="Secondary" contained={true} border={true} style={{ paddingRight: 0, paddingLeft: 0 }}>
	<p>A subtle button used for secondary actions, such as "Cancel".</p>
	<Preview wrapper={true}>
		<Button theme="secondary">Secondary button</Button>
	</Preview>
	<Snippet language="js">{SecondaryButtonCode}</Snippet>
</Section>

<Section title="Tertiary" contained={true} style={{ paddingRight: 0, paddingBottom: 0, paddingLeft: 0 }}>
	<p>Link style button commonly used as navgiation menu.</p>
	<Preview wrapper={true}>
		<Button theme="tertiary">Tertiary button</Button>
	</Preview>
	<Snippet language="js">{TertiaryButtonCode}</Snippet>
</Section>
