import { Canvas, Source } from '@storybook/addon-docs';
import { Docs } from '@wpmudev/docs-elements';
import { Grid as SuiGrid } from '../components/DynamicGrid';

import dedent from 'dedent';

export const codeRow = dedent`
<div class="sui-row"> ... </div>
`;

export const codeRowInline = dedent`
<!--
Min container width:
None (Auto)
-->
<div class="sui-row sui-row--inline"> ... </div>
`;

export const codeBreakpoints = dedent`
<!--
@param {String} $bp - Sets the minimum screen size code.
@param {String} $dir - Sets the columns direction.
-->
<div class="sui-row sui-row-$bp--$dir"> ... </div>
`;

<span></span>

<Docs.Section>
	<p>
		Rows are wrappers for columns. Each column has some padding (called a
		gutter) for controlling the space between them. The rows fight back
		columns padding with negative margins; this way, all the columns
		visually align down the left side.
	</p>
	<p>
		Enable row styles and align columns properly; for that, use the class
		name as in the sample code below:
	</p>
	<Source dark language="html" code={codeRow} />
</Docs.Section>

<Docs.Section title="Modifiers">
	<p>
		Rows can organize columns inline (placing one next to each other) or
		stacked (placing one below each other) based on different breakpoints to
		create a more flexible layout.
	</p>
	<p>
		Columns are aligned one below the other, stacked mode, by default. To
		switch them into inline columns for the window starting point (zero),
		you need to:
	</p>
	<Source dark language="html" code={codeRowInline} />
	<p>
		Since the grid system departs from small to large screens, you need to
		use a modifier class to make the proper variations based on the minimum
		screen size you want to change.
	</p>
	<p>
		For that, you need to use <code>sui-row-$bp--$dir</code> class, where{' '}
		<code>$bp</code> is the breakpoint code (read Breakpoints section for
		more) and <code>$dir</code> for the alignment direction, which could be{' '}
		<strong>inline</strong> or <strong>stacked</strong>.
	</p>
	<Source dark language="html" code={codeBreakpoints} />
</Docs.Section>

<Docs.Section title="Breakpoints">
	<p>
		The breakpoints divide into five different sizes, apart from the default
		or initial size, based on the WordPress admin content area sizing
		changes and elements like the sidebar menu or top menu that might
		interrupt the content area depending on the browser's maximum width.
		These breakpoints state as follows:
	</p>
	<Docs.Table2
		head={['', 'Default', 'Small', 'Medium', 'Large', 'Extra Large']}
	>
		<tr>
			<th scope="row">Min container width</th>
			<td>None (auto)</td>
			<td>≥ 783px</td>
			<td>≥ 1024px</td>
			<td>≥ 1200px</td>
			<td>≥ 1600px</td>
		</tr>
		<tr>
			<th scope="row">Class prefix</th>
			<td>-</td>
			<td>
				<code>.sui-row-sm--</code>
			</td>
			<td>
				<code>.sui-row-md--</code>
			</td>
			<td>
				<code>.sui-row-lg--</code>
			</td>
			<td>
				<code>.sui-row-xl--</code>
			</td>
		</tr>
		<tr>
			<th scope="row">Stacked alignment</th>
			<td>Default</td>
			<td>✓</td>
			<td>✓</td>
			<td>✓</td>
			<td>✓</td>
		</tr>
		<tr>
			<th scope="row">Inline alignment</th>
			<td>✓</td>
			<td>✓</td>
			<td>✓</td>
			<td>✓</td>
			<td>✓</td>
		</tr>
	</Docs.Table2>
	<p>
		The modifier classes affect the minimum container width, as stated in
		the table above.
	</p>
</Docs.Section>
