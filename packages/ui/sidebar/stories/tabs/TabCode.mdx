import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection } from "@wpmudev/sui-docs"

<PackageInstallGuide packageName="@wpmudev/sui-sidebar" />

<Section title="Props" border={true}>
	<PropSection title="children" type="React Node" defaultValue="{{default}}">
		<p>The content of the Sidebar component </p>
	</PropSection>

    <PropSection title="className" type="string" defaultValue="{{default}}">
    	<p>Custom css className(s) </p>
    </PropSection>

</Section>

{/* * Use this for sub components */}

<Section
	title="Sub Components"
	contained={true}
	style={{ paddingTop: 30, paddingBottom: 0 }}
>
		<p>The Sidebar component offers a collection of nested subcomponents to be utilized within it. These subcomponents include:</p>

    <Section title="SidebarItem" contained={true} style={{ paddingTop: 0, paddingLeft: 20, paddingRight: 0 }}>
    	<p>
    		The <Code small={true}>SidebarItem</Code> component represents a single item in the Sidebar, you can add as many items as needed
    	</p>

    	<PropSection title="url" type="string" defaultValue="{{default}}">
    		<p>
    			The url associated with the SidebarItem
    		</p>
    	</PropSection>

    	<PropSection title="icon" type="string" defaultValue="{{default}}">
    		<p>
    			An optional icon to be displayed within the SidebarItem
    		</p>
    	</PropSection>

    	<PropSection title="title*" type="string" defaultValue="{{default}}">
    		<p>
    			The title or the text of the item
    		</p>
    	</PropSection>

    	<PropSection title="isActive" type="boolean" defaultValue="{{default}}">
    		<p>
    			Whether the sidebar item is active or not
    		</p>
    	</PropSection>

    	<PropSection title="action" type="ReactNode" defaultValue="{{default}}">
    		<p>
    			An optional action component to be displayed alongside the title in the SidebarItem.
    		</p>
    	</PropSection>

    	<PropSection title="isDisabled" type="boolean" defaultValue="{{default}}">
    		<p>
    			When set to <Code small={true}>true</Code> the item will be disabled.
    		</p>
    	</PropSection>

    	<PropSection title="onClick" type="boolean" defaultValue="{{default}}">
    		<p>
    			A callback function to be invoked when the item is clicked
    		</p>
    	</PropSection>

    	<PropSection title="className" type="string" defaultValue="{{default}}">
    		<p>
    			Custom css className(s)
    		</p>
    	</PropSection>

    </Section>

    <Section title="SidebarDropdown" contained={true} style={{ paddingTop: 0, paddingLeft: 20, paddingRight: 0 }}>
    	<p>
    		This's the Sidebar dropdown menu that appear on mobile, This component could have multiple instances of the <Code small={true}>SidebarItem</Code> component to be displayed on mobile only
    	</p>

    	<PropSection title="children" type="React Node" defaultValue="{{default}}">
    		<p>
    			<p> The content of the Dropdown </p>
    		</p>
    	</PropSection>

    	<PropSection title="className" type="string" defaultValue="{{default}}">
    		<p>
    			Custom css className(s)
    		</p>
    	</PropSection>

    	<PropSection title="selectedItemName" type="string" defaultValue="{{default}}">
    		<p>
    			The currently selected item title
    		</p>
    	</PropSection>
    </Section>

</Section>
