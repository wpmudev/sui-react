import { Section, Row, Col, Table, List, Code, Tag, PackageInstallGuide, PropSection } from "@wpmudev/sui-docs"

<PackageInstallGuide packageName="@wpmudev/sui-dropdown" />

<Section title="Props" border={true}>

    	<PropSection title="Label" type="string" defaultValue="{{default}}">
    		<p>The dropdown label</p>

    	</PropSection>

    	<PropSection title="isSmall" type="boolean" defaultValue="{{default}}">
    		<p>If set to <Code isSmall={true}> true </Code> the dropdown menu will have a smaller width</p>
    	</PropSection>

    	<PropSection title="isLabelHidden" type="boolean" defaultValue="{{default}}">
    		<p>If set to <Code isSmall={true}> true </Code> the label won't be displayed</p>
    	</PropSection>

    	<PropSection title="isFixedHeight" type="boolean" defaultValue="{{default}}">
    		<p>Whether the dorpdown has a fixed height with a scroll bar for overflow or it adapts its height to the content.  </p>
    	</PropSection>

    	<PropSection title="current" type="number" defaultValue="{{default}}">
    		<p>The current selected value </p>
    	</PropSection>

    	<PropSection title="children" type="React Node" defaultValue="{{default}}">
    		<p>The content that goes inside the Dropdown </p>
    	</PropSection>

    	<PropSection title="direction" type="string" defaultValue="{{default}}">
    		<p>Dropdown popover location: <Code small={true}>left</Code> or <Code small={true}> right</Code> </p>
    	</PropSection>

    	<PropSection title="buttonIcon" type="string" defaultValue="{{default}}">
    		<p>The Drop down button icon name.</p>
    		<p> You can check available icons on <a href="https://github.com/wpmudev/sui-icons" target="_blank"> sui-icon</a> package</p>
    	</PropSection>

    	<PropSection title="onMenuClick" type="function" defaultValue="{{default}}">
    		<p>A callback function to be invoked when an item is clicked, it has the following parameters in order: </p>
    		<List>
    			<span label={
    				<>
    					<Code small={true}>id</Code>
    					 The clicked element id
    				</>
    				} />
    			<span label={
    				<>
    					<Code small={true}>event</Code>
    					 The click event object
    				</>
    				} />
    		</List>
    	</PropSection>

    	<PropSection title="menu" type="array" defaultValue="{{default}}">
    		<p>The menu items to be rendered, each item is an object that could be either a <strong>Menu Group</strong> or a <strong>Menu Item</strong>. </p>

    		<Section
    			title={{ content: "Menu Item*",small: true }}
    			style={{ paddingTop: 0, paddingLeft: 20, paddingRight: 0 }}
    		>
    			<p>The menu Item object has the following properties </p>
				<List>
					<span label={
						<>
							<Code small={true}>id</Code> a unique id for the item
						</>
						} />
					<span label={
						<>
							<Code small={true}>label</Code>  represents the item title
						</>
						} />
					<span label={
						<>
							<Code small={true}>props</Code> An object representing the props of the <a href="/docs/sui-components-core-menu--docs" target="_blank">Menu component</a> with the exception of <Code small={true}> children </Code>
						</>
						}
					/>
				</List>
			</Section>

    	</PropSection>

    		<Section
    			title={{ content: "Menu Group*",small: true }}
    			style={{ paddingTop: 0, paddingLeft: 20, paddingRight: 0 }}
    		>
    			<p>The menu group object has the following properties </p>

    			<List>
    				<span label={
    					<>
    						<Code small={true}>id</Code> a unique id for the group
    					</>
    				} />

    				<span label={
    					<>
    						<Code small={true}>label</Code> represents the group title
    					</>
    				} />

    				<span label={
    					<>
    						<Code small={true}>menus</Code> An array of menu items
    					</>
    				} />
    			</List>
    		</Section>
  

</Section>
