import { Section, Preview, Snippet, Code } from '@wpmudev/storybook';
import { Button } from '../../../lib/react-button';
import dedent from 'dedent';

export const DisabledButtonCode = dedent`
import React from 'react';\n
import { Button } from '@wpmudev/sui-react/button';\n
const Example() => {
	return <Button disabled={true}>Disabled button</Button>;
}\n
export default Example;
`;

export const LoadingButtonCode = dedent`
import React from 'react';\n
import { Button } from '@wpmudev/sui-react/button';\n
const Example() => {
	return <Button loading={true}>Loading button</Button>;
}\n
export default Example;
`;

<Section title="Disabled" contained={true} border={true} style={{ paddingRight: 0, paddingBottom: 0, paddingLeft: 0 }}>
	<p>Set <Code>isDisabled</Code> to disable a button that isn't usable.</p>
	<Preview wrapper={true}>
		<Button disabled={true}>Disabled button</Button>
	</Preview>
	<Snippet language="js">{DisabledButtonCode}</Snippet>
</Section>

<Section title="Loading" contained={true} border={true} style={{ paddingRight: 0, paddingLeft: 0 }}>
	<p>Set <Code>isLoading</Code> to indicate the button is loading. The button text is hidden and a spinner is shown in its place.</p>
	<Preview wrapper={true}>
		<Button loading={true}>Loading button</Button>
	</Preview>
	<Snippet language="js">{LoadingButtonCode}</Snippet>
</Section>
