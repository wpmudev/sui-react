import { Section, Preview, Snippet, Code } from '@wpmudev/storybook';
import { Recipient } from '../../../src';
import dedent from 'dedent';

export const ConfirmedRecipientCode = dedent`
import React from 'react';\n
import { Recipient } from '@wpmudev/sui-react';\n
const Example() => {
	return <Recipient userName="Recipient name" userEmail="email@example.com" userConfirmed={true} />;
}\n
export default Example;
`;

export const InvitedRecipientCode = dedent`
import React from 'react';\n
import { Recipient } from '@wpmudev/sui-react';\n
const Example() => {
	return <Recipient userName="Recipient name" userEmail="email@example.com" isInvited={true} />;
}\n
export default Example;
`;

<Section
	title="Confirmed"
	contained={true}
	border={true}
	style={{ paddingRight: 0, paddingLeft: 0 }}
>
	<p>
		Set the <Code small={true}>userConfirmed</Code> prop to
		<Code small={true}>true</Code>.
	</p>
	<Preview wrapper={true}>
		<Recipient userName="Recipient name" userEmail="email@example.com" userConfirmed={true} />
	</Preview>
	<Snippet language="js">{ConfirmedRecipientCode}</Snippet>
</Section>

<Section
	title="Invited"
	contained={true}
	border={true}
	style={{ paddingRight: 0, paddingLeft: 0 }}
>
	<p>
		Set the <Code small={true}>isInvited</Code> prop to
		<Code small={true}>true</Code>.
	</p>
	<Preview wrapper={true}>
		<Recipient userName="Recipient name" userEmail="email@example.com" isInvited={true} />
	</Preview>
	<Snippet language="js">{InvitedRecipientCode}</Snippet>
</Section>
