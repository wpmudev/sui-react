import { Section, Switcher, Preview, Snippet, Code } from "@wpmudev/sui-docs"
import { CodeEditor } from "../../src"
import dedent from "dedent"

import Languages from "./Examples/Languages.mdx"
import LinesNumbers from "./Examples/LinesNumbers.mdx"
import ReadOnly from "./Examples/ReadOnly.mdx"
import FileName from "./Examples/FileName.mdx"
import CopyButton from "./Examples/CopyButton.mdx"

export const reactSampleCode = `import React from 'react';

const MyComponent = ({ name }) => {
    return (
        <div>
            <h1>Hello, {name}!</h1>
            <p>This is a basic React functional component.</p>
        </div>
    );
};

export default MyComponent;
`


export const sampleCode = dedent`
    <CodeEditor language="javascript">
        import React from 'react';

        const MyComponent = ({ name }) => {
            return (
                <div>
                    <h1>Hello, {name}!</h1>
                    <p>This is a basic React functional component.</p>
                </div>
            );
        };

        export default MyComponent;
    </CodeEditor>
`

<Section contained={true} style={{ paddingTop: 30 }} border={true}>
	<p>The default form of the CodeEditor component.</p>
	<p>Please make sure to indicate the "language" property with the appropriate code language to enable accurate syntax highlighting.</p>
	<Preview wrapper={true}>
		<CodeEditor language="javascript">
            {reactSampleCode}
		</CodeEditor>
	</Preview>
	<Snippet language="js">{sampleCode}</Snippet>
</Section>

<Section title="More Examples" style={{ paddingTop: 30 }}>
	<Switcher>
		<div label="Languages" value="languages">
			<Languages />
		</div>
		<div label="File Name" value="filename">
			<FileName />
		</div>
		<div label="Lines Number" value="lines-number">
			<LinesNumbers />
		</div>
		<div label="Read Only" value="read-only">
			<ReadOnly />
		</div>
		<div label="Copy Button" value="copy-button">
			<CopyButton />
		</div>
	</Switcher>
</Section>
