import { Meta, Canvas, Story, Source } from '@storybook/addon-docs';
import { Docs } from '@wpmudev/docs-elements';
import { GridSample } from './Grid';
import dedent from 'dedent';

import '../src/layout.scss';

export const srcDemoCode = dedent`
<div class="sui-row sui-row-md--inline">\n
	<div class="sui-col sui-col--3"> ... </div>\n
	<div class="sui-col sui-col--3"> ... </div>\n
	<div class="sui-col sui-col--3"> ... </div>\n
</div>
`;

export const srcRow = dedent`
<div class="sui-row"> ... </div>
`;

export const srcRowInline = dedent`
<!--
Min container width:
None (Auto)
-->
<div class="sui-row sui-row--inline"> ... </div>
`;

export const srcRowBreakpoints = dedent`
<!--
@param {String} $bp - Sets the minimum screen size code.
@param {String} $dir - Sets the columns direction.
-->
<div class="sui-row sui-row-$bp--$dir"> ... </div>
`;

export const srcRowSampleBp = dedent`
<!--
Min container width:
Tablet (≥ 783px and larger)
-->
<div class="sui-row sui-row-sm--inline"> ... </div>
<div class="sui-row sui-row-sm--stacked"> ... </div>\n
<!--
Min container width:
Laptop (≥ 1024px and larger)
-->
<div class="sui-row sui-row-md--inline"> ... </div>
<div class="sui-row sui-row-md--stacked"> ... </div>\n
<!--
Min container width:
Retina Laptop (≥ 1200px and larger)
-->
<div class="sui-row sui-row-lg--inline"> ... </div>
<div class="sui-row sui-row-lg--stacked"> ... </div>\n
<!--
Min container width:
Desktop (≥ 1600px and larger)
-->
<div class="sui-row sui-row-xl--inline"> ... </div>
<div class="sui-row sui-row-xl--stacked"> ... </div>\n
`;

<Meta title="SUI/CSS Framework/Layout/Grid" />

<Docs.Page title="Grid System" light={true} status="ready">
	<Docs.Content
		title={
			<>
				Our robust mobile-first grid system builds layouts of all shapes
				and sizes thanks to a twelve-column grid system based on flexbox
				with several predefined classes, Sass variables, and mixins.
			</>
		}
	>
		<Docs.Section title="How it works">
			<p>
				The Shared UI grid system employs a sequence of rows, and
				columns to align and organize the content inside the layout. The
				grid is created using{' '}
				<a
					href="https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Flexible_Box_Layout/Using_CSS_flexible_boxes"
					target="_blank"
					rel="nofollow"
				>
					flexbox
				</a>{' '}
				and is fully responsive.
			</p>
			<Canvas withToolbar>
				<div className="sui-wrap sui-theme--light">
					<GridSample alignMD="inline" size="4" />
				</div>
			</Canvas>
			<Source dark language="html" code={srcDemoCode} />
			<p>
				Above, there's an example of three equal-width columns on small,
				medium, large, and extra large devices, achieved by using our
				predefined grid classes.
			</p>
		</Docs.Section>
		<Docs.Section title="Row">
			<p>
				Rows are wrappers for columns. Each column has some padding
				(called a gutter) for controlling the space between them. The
				rows fight back columns padding with negative margins; this way,
				all the columns visually align down the left side.
			</p>
			<p>
				Enable row styles and align columns properly; for that, use the
				class name as in the sample code below:
			</p>
			<Source dark language="html" code={srcRow} />
			<Docs.Section title="Modifiers" nested={true}>
				<p>
					Rows can organize columns inline (placing one next to each
					other) or stacked (placing one below each other) based on
					different breakpoints to create a more flexible layout.
				</p>
				<p>
					Columns are aligned one below the other, stacked mode, by
					default. To switch them into inline columns for the window
					starting point (zero), you need to:
				</p>
				<Source dark language="html" code={srcRowInline} />
				<p>
					Since the grid system departs from small to large screens,
					you need to use a modifier class to make the proper
					variations based on the minimum screen size you want to
					change.
				</p>
				<p>
					For that, you need to use <code>sui-row-$bp--$dir</code>,
					where <code>$bp</code> is the breakpoint code (read
					Breakpoints section for more) and <code>$dir</code> for the
					alignment direction, which could be <strong>inline</strong>{' '}
					or <strong>stacked</strong>.
				</p>
				<Source dark language="html" code={srcRowBreakpoints} />
				<p>
					Below is a list of all available variations for inline and
					stacked alignments based on available breakpoints.
				</p>
				<Source dark language="html" code={srcRowSampleBp} />
			</Docs.Section>
		</Docs.Section>
		<Docs.Section title="Column">
			<p>
				Column classes organize the columns out of the amount you need
				per row based on the twelve-column grid size; this means, if you
				need to display three equal-width columns, you must divide the
				number of columns by the size of the grid, which in this case is
				twelve. The result you get is the column size.
			</p>
			<p>
				Based on the example above, you can use the .sui-col--4 class
				name to display three equal-width columns since three divided by
				twelve results in four, which means each column needs to occupy
				four spaces in the grid.
			</p>
		</Docs.Section>
	</Docs.Content>
	<Docs.Footer />
</Docs.Page>
